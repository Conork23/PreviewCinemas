@model IEnumerable<PreviewCinemas.Models.Screen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScreenNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seats)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScreenNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seats)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScreenID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScreenID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScreenID })
        </td>
    </tr>
}

</table>
